dataset_mode: raw  # or "raw"
device: cuda  # auto|cpu|cuda
window: 48
hidden_size: 8
num_layers: 1
dropout: 0.1
epochs: 30
learning_rate: 0.001
batch_size: 16
meter_embedding_dim: 16
early_stopping_patience: 6
features:
  calendar: true
  cyclic: true
  holiday_flags: true
  target_lags: [1, 24, 168]
tune_grid:
  window: [48]
  hidden_size: [8]
  learning_rate: [0.001, 0.0005]